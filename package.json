{
  "name": "redis-cache-heatmap",
  "version": "1.0.0",
  "description": "Redis Cache Heatmap - 3D visualization of cache hits vs misses",
  "private": true,
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "build": "mkdir -p build && cd build && cmake .. && make",
    "dev": "npm run build && concurrently \"./build/redis-server\" \"npm run dev --prefix server\" \"npm run dev --prefix web\"",
    "deploy": "./deploy.sh",
    "install:all": "npm install && npm install --prefix server && npm install --prefix web"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
