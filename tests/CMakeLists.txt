# Test configuration for ScuffedRedis
cmake_minimum_required(VERSION 3.10)

# Enable testing
enable_testing()

# Add test executables as we create them
# Example:
# add_executable(test_hashtable test_hashtable.cpp ../src/data/hashtable.cpp)
# add_test(NAME hashtable_test COMMAND test_hashtable)
